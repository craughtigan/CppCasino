// Class to contain many cards that form a deck
#include <string>
#include "Card.h"

class Deck {
public:
	// function to remove the top card from the deck and return in
	Card getTopCard() {
		
	}
	// function to add a card to the top of the deck
	void addCardOnTop(Card card) {

	}
	// function to add a card to the bottom of the deck
	void addCardOnBottom(Card card) {

	}
};